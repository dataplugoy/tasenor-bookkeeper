description: User has not been authenticated.
content:
  application/json:
    schema:
      properties:
        message:
          description: Error message.
          type: string
          example: Unauthorized.
      required:
        - message
